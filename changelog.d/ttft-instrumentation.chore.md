Instrumentación de latencia del chat local (archima): la línea de log de cada turno ahora trae `ttft:Xs gen:Ys` (TTFT ≈ prefill, la única señal warm/cold —el `cache:X/Y` es siempre 0/0 en backend local porque vLLM no reporta cached_tokens), y el cold session-open loguea `⏱ open:Xs mcp:Ys` para separar el costo de arranque/MCP del de prefill. Solo-logging, sin cambio de comportamiento. Ver quickboot/mediciones.md.
