El cold-open del chat local (archima) baja otro escalón: las llamadas del gateway a gpuhost (cp.sh state/serve/wiki-setup + las 5 del Agent Vault de prepareSession) ahora multiplexan sobre UNA conexión ssh persistente (ControlMaster). El handshake ssh (~2s por llamada) se pagaba en cada exec → ~16s del reopen eran handshakes seriales; ahora sólo la 1ª llamada lo paga y el resto reusa el master.
