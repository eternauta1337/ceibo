REM (consolidación de wikis) ahora planifica con **gemma local** por default en vez de sonnet: corre 100% on-box y gratis (el planner sonnet queda opt-in vía `REM_PLANNER_PROVIDER=anthropic`). Se sumó un **sandbox dry-run** (`pnpm --filter @ceibo/rem-runner run:dry <wiki> --local <dir>`) para ver qué planificaría/haría REM sin pushear nada, el parser de planes ahora tolera el JSON con escapes octales que emite gemma, y se eliminó la pasada semanal (era redundante con la diaria).
