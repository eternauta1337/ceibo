El cold-open del chat local (archima) es bastante más rápido: el reconcile de los MCP pasó de conectar en SERIE con pacing de 1.5s (medido: 9 MCP = ~19.8s, el grueso del primer mensaje del día tras el clear de 4am) a PARALELO ACOTADO (cap de concurrencia < 10, bajo el bucket per-agente del Agent Vault) → ~3-4s. El verify+retry de 429/SSE-flaky sigue de backstop. Tunable por `ARCHIMA_MCP_CONCURRENCY` (default 8).
